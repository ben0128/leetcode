## 讀書計畫 #7 — 2026-05-24
主題重點：Sliding Window 鞏固、Hash+Prefix Sum 鞏固、DP 2D SR（39 天 overdue）、DFS Cycle Detection gap、Heap+LL Hard

| # | 題目 | 難度 | 主題 | 為什麼選這題 |
|---|------|------|------|-------------|
| 1 | 3. Longest Substring Without Repeating Characters | Medium | Sliding Window | weak（只做過 #76 一題）；🔥🔥🔥 Google 高頻；經典 hashmap-position 變體 sliding window（與 #76 counter-based 不同子類）|
| 2 | 525. Contiguous Array | Medium | Hash + Prefix Sum | weak（只做過 #560 一題）；🔥🔥；**±1 transform 巧妙**（0→-1 → 變「prefix sum 相等的兩 index 距離最大」）；鞏固 #560 same-prefix lookup pattern |
| 3 | 97. Interleaving String (SR) | Medium | 2D DP | **SR 39 天 overdue**（與 #295 並列 earliest due，2026-04-15）；Plan #1 ⚠️「第一次做 2D DP，需引導才能起頭」；DP 2D **weak**（最近兩題 ⚠️⚠️：64 + 97）|
| 4 | 210. Course Schedule II | Medium | Graph / DFS Cycle Detection | **gap**：3-color/3-state DFS cycle detection 完全沒接觸過（ANALYSIS P1 #7）；#207 SR 寫的是 Kahn's BFS，**這次強制用 DFS** 補洞；🔥🔥 |
| 5 | 23. Merge K Sorted Lists | Hard | Heap + Linked List | 🔥🔥🔥 Google 高頻 Hard；Heap borderline（最近 ✅⚠️）+ Linked List 沒鞏固；經典「heap of size k」+「dummy head」雙 pattern |

建議順序：先做 #1（sliding window 暖身鞏固）→ #2（prefix sum 鞏固，同 #1 都是 hashmap 應用 ramp-up）→ #3（SR DP 2D 中段）→ #4（gap 新概念）→ #5（Hard heap+LL 收尾）
預估總時間：2-2.5 小時（4 M + 1 H；目標：M < 20 min、H < 30 min）

**設計考量**
- Interleaving：Sliding Window → Hash+Prefix → DP-2D → Graph-DFS → Heap+LL（5 個不同主題，無連續同主題 ✓）
- 難度分布：4 Medium + 1 Hard ✓
- Plan #6 主題重疊檢查：
  - P6 主題：BS / DP-1D / Trie / Tree+Graph-BFS / Monotonic Queue
  - P7 主題：Sliding Window / Hash+Prefix / DP-2D / Graph-DFS / Heap+LL
  - 重疊：DP（不同子類：1D vs 2D）、Graph（不同子類：Tree→Graph BFS vs Topo Sort DFS）= 2/10 = 20% ≤ 40% ✓
- Weak 槽位：#1（Sliding Window 鞏固）、#2（Hash+Prefix 鞏固）、#3（DP 2D SR + weak）、#5（Heap borderline）
- Gap 槽位：#4（DFS cycle detection 首次）
- SR 槽位：#3（97 earliest due，39 天 overdue）
- Google 校準：#1 #5 都 🔥🔥🔥

**🔴 Plan #7 新硬規則：思路 4-段強制**

連續 4 題 (#139 #208 #863 #239) 思路偏薄記錄在案——一律一句話帶過、invariant 不寫進檔案、明確要求才補。

**從 Plan #7 起，每題 code 前必須口述完整 4-段思路**：
1. **演算法骨架**（核心操作流程）
2. **資料結構選擇 + 理由**（為什麼這個結構讓 X 操作 O(1)/O(log n)）
3. **Invariant**（**狀態描述**，不是步驟描述；每次迴圈尾/某時間點什麼條件成立）
4. **複雜度**（Time + Space，含 sentinel/auxiliary structure 的成本）

**未滿足 4 段 → 自動 ⚠️**，不管 code 對錯。Code 跑完後檔案 `思路：` 區塊也必須含這 4 段，不只口述。

這條規則是 forcing function——把「思路偏薄」這個 chronic 弱項從「事後提醒」改成「事前 gate」。

**SR backlog 警示**

SessionStart hook 顯示 **26 個 SR 項目 overdue**（最早 2026-04-15，今天 2026-05-24，已積 39 天）。每份 plan 只塞 1 個 SR 槽位，速度跟不上排程節奏——Plan #6 結束後純 SR 已達 26 個。

短期策略：Plan #7 維持 1 SR 槽位（避免單一 plan 太重）；但 Plan #8 起考慮：
- 加開 **SR-only 短 plan**（每週 1 次，純做 3-5 個 due 項目）
- 或拉長 plan 槽位（5 → 6/7 題，多塞 SR）
- 或改 SR 排程規則（過 30d overdue 自動退階到 base，避免 backlog 太重複習負擔）

**開始前的暖身問題**

#1（3 Longest Substring W/O Repeat）之前：
1. 「最長無重複子字串」這題 brute force 是 O(n²) 還 O(n³)？降到 O(n) 你會用什麼資料結構？
2. Sliding window 兩種記錄方式：(a) 記字元「**最後出現位置**」(b) 記「**出現次數**」——這題哪個更乾淨？為什麼？（提示：第二種要 shrink 直到 count=1，第一種可以 jump）

#2（525 Contiguous Array）之前：
3. 0 和 1 數量相等 → 把 0 視為 -1，1 視為 +1，問題等價於什麼？（提示：prefix sum）
4. 等價問題轉換後，跟 #560 Subarray Sum K 有什麼一樣、哪裡不一樣？lookup 公式是什麼？
5. `{0: -1}` 這個 init 為什麼是 -1 不是 0？（這跟 #560 的 `{0: 1}` 哲學不同——一個記「次數」一個記「位置」，想清楚）

#3（97 Interleaving String SR）之前：
6. `dp[i][j]` 定義你會怎麼設？base case 是什麼？轉移時，這格的 True 來自哪 2 個方向？
7. Space 能優化到 O(min(m, n)) 嗎？（Plan #1 時你寫到 O(n)，這次直接挑戰 O(min(m,n))）
8. 為什麼這題不是 backtracking 而是 DP？（提示：overlapping subproblems）

#4（210 Course Schedule II）之前：
9. #207 SR 你寫的是 Kahn's BFS（in-degree + queue）。這次**強制用 DFS**——DFS 怎麼偵測 cycle？(提示：3-color / 3-state)
10. white / gray / black 三色各代表什麼狀態？什麼時候從 white → gray？gray → black？什麼條件下偵測到 cycle？
11. DFS 版怎麼產生 topological order？（提示：跟 black 化的順序有關，但要反過來）

#5（23 Merge K Sorted Lists）之前：
12. K 條 linked list 合併，brute force（全部攤平 sort）O(N log N)。用 heap 怎麼降到 O(N log K)？K 是什麼？
13. Python heap 不支援 custom comparator，linked list node 不能直接比——怎麼解？（提示：tuple 第二項放什麼）
14. Follow-up：不用 heap 還有什麼解法？（提示：divide & conquer 兩兩合併）

---

## 進度追蹤

| # | 題目 | 結果 | 花費時間 | 筆記 |
|---|------|------|----------|------|
| 1 | 3. Longest Substring Without Repeating Characters | ✅ | ~15 min | Sliding window + hashmap (last-seen index) 一次過 6 cases。**Plan #7 4-段思路 forcing function 起作用**：第 1 段骨架初版跳過、第 3 段 invariant 初寫不完整（誤稱 hashmap 也無 dup），經 Socratic push 後補完兩條 clean invariant：(1) `[l,r]` 內無 dup (2) hashmap[c]=c 最近 index（不管在不在窗）。**「abba」trap 一發點破**：student 主動講出「stale index 視為無效」→ 翻譯成 `if hashmap[c] >= l` guard。**Code 風格 trade-off**：student 把 `maxLen=max(...)` 只放 else 分支（clever，需證「shrink 後新窗 ≤ 舊窗 ≤ maxLen」），選 Version A 過 Version B（defensive 標準寫法）。挑戰後仍選 A 推上去——**Plan #8 起遇類似 trade-off 預設選 B**。Q1 證明初版不完整（只說「l 移右 ≥1」漏比較對象 r），補完整證明後 OK。Space O(min(n,|charset|)) 答對。**SR 重做要點**：(1) 開口直接 4 段思路含 invariant（不要再被 push） (2) 預設用 Version B（max 放 if/else 外） (3) Space 直接答 O(min(n,m)) (4) `if hashmap[c] >= l` guard 為何需要要能口頭講 stale-index 證明 |
| 2 | 525. Contiguous Array | ⚠️ | ~30 min (15 思路 + 15 code) | **Hash+Prefix Sum 第 2 題（#560 後變體）**。Code 一次過 6 cases，無 hint。**±1 transform 認對**（Q1 一答即「subarray 和 = 0」）。**但中段卡 hashmap 語意**：Q3 直接說「我不太懂 hashmap 要存哪些」，需 Socratic 拆 4 步（等式 `P[j+1]=P[i]` → key/value 各自存什麼 → 最早 vs 最晚 index → init `{0:-1}` 反例）才補完。**Init `{0:-1}` 詮釋偏鬆**：第一版「從頭選到尾」只抓特例（全陣列），精修為「還沒處理任何元素的虛擬狀態 = prefix 0 在 index -1」。**4-段思路 forcing function 連 2 題被 push**：第 1 段漏 transform、第 4 段 Big-O 太鬆需具體寫；學生口頭講出全部但動手寫卻說「幫我補好」——被拒絕後自己貼 + 修。**Catch 助教錯誤 ✓**：我說 Space O(2n+1)，學生反駁「最多 N 種，全 1 的情況下」——對；精準版 n+1 entries（每步 ±1，n+1 snapshot 不可能踩滿 2n+1 範圍）。**過時間 30 vs 15 目標**：因為已做 #560，這題該是 refresher，但中段卡住反映 prefix-sum-hashmap value 語意（index vs count）還沒真懂。**vs #560 對比**：#560 hashmap value=count（為了數「幾個」）；#525 hashmap value=earliest index（為了找「最長」+ 不 overwrite）。 |
| 3 | 97. Interleaving String (SR) | ⚠️ | ~25 min | **39 天 overdue SR 重做**。Code 一次過 6 cases（含 length mismatch、雙空字串、單空字串）。**vs Plan #1 ⚠️「邊界需引導」明顯進步**：自己起頭、自己寫對 swap-to-shorter + 第 0 row 初始化 + inner-loop 1D 滾動讀寫順序。**Q1-Q3 一答即過**：greedy 失敗（多種可能）、dp[i][j] 定義、轉移方程 OR 兩方向 ✓。**Q4 O(min) 優化卡住**：第一版只說「rolling 1 row」= O(n)，被追問「短的那條」才答出「外迴圈跑長的」O(min(m,n))。**Q5「為什麼 DP 不 backtracking」答得鬆**：「太長會超時」抓到結果但講不出病因「overlapping subproblems」，助教展開 trace `helper(1,1)` 兩路徑撞、`(i,j)` 被 `C(i+j,i)` 條路徑撞、10⁴ vs 10⁶⁰ 數量級對比後學生主動要求「幫我記起來下次問我一次」→ 已存記憶 feedback_dp_overlapping_subproblems_quiz。**4-段 forcing function 半破**：學生口述完成 4 段但動手寫只貼 Section 1（演算法骨架），Section 2/3 完全沒寫，被點名後才補。Section 1 用助教 hint 直接貼進去（不夠原創）。Section 3 invariant「讀寫順序」是這題 1D 滾動的關鍵 — 學生 catch 對。**Code 細節**：swap 後 m,n 同步交換 ✓、第 0 row 用 break 早停 ✓、inner loop `for j in range(n)` 對應 dp[j+1] 寫法乾淨。**SR 重做要點**：(1) Q4 O(min) 開口直接答「外迴圈跑長的」不要被問 (2) Q5 開口直接「overlapping subproblems」+ 舉 helper(1,1) 兩路徑 trace (3) 4 段思路一寫就 4 段完整，不要只寫 Section 1 (4) Section 3 invariant 開口直接講「讀 dp[j+1]=上一 row、讀 dp[j]=當前 row」 (5) `helper(1,1) 兩路徑撞` 數量級對比口頭講出 (6) 為何「短在內」省空間：rolling 紀錄一條 row，row 長度 = 內維 = n → 確保 n=min |
| 4 | 210. Course Schedule II | ⚠️ | ~40 min (含 debug) | **DFS 3-color cycle detection gap 首次**（#207 寫過 Kahn's BFS，這次強制 DFS）。**思路 4 段大進步**：edge 方向（prereq→dependent）+ 三色 invariant（灰=當前路徑、黑=完成可剪枝）+ 為什麼不能合併成 visited + 複雜度 O(n+m) 都對。**Cycle 偵測 insight 一次命中**：碰到灰=back edge=環。**但 code 第一版有真 bug**：自己多加 in-degree 預處理，DFS **只從入度 0 的點起跑** → 環裡的 node 入度恆 ≥1 永遠不在起點集 → **不相連的環漏判**（case A `[[1,2],[2,1]]` 回 `[0]` 應 `[]`；case B 純環回 `[]` 純屬僥倖 ans 空）。"All tests passed!" **假 pass** 再現（同 #33）。Socratic「環裡 node 入度可能 0 嗎」引導 → 拿掉 in-degree 機關改「for 每個 white node → dfs」修好。**length check `len==numCourses else []` 自己判斷為 dead code 並移除**（推理正確 ✓）。**🔴 test 紀律重大缺口**：明確要求加 regression test（case A）**連續 4 次未加**，最後助教代加。**為什麼 post-order+reverse 不是 pre-order**：本來「會寫講不出」，用 `0→2`/`1→2` 反例講通（黑色=下游全就位的保證，灰色只是剛到）→ 已寫進 notes/dfs_topological_sort.md。**SR 重做要點**：(1) **寫前先想清楚外層 loop 從每個 white node 起**，不要加 in-degree 機關（多餘又是 bug 源）(2) **自己加 regression test**（尤其環在獨立 component 的 case）不要等被點名 (3) 思路 Section 3 invariant 寫「狀態」（灰=當前路徑、黑=子孫全完）不寫步驟 (4) 開口能講 post-order+reverse 的因果（黑=下游就位）(5) edge 方向 ↔ reverse 綁死要先定死 |
| 5 | 23. Merge K Sorted Lists | — | — | — |
