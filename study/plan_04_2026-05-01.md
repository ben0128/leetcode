## 讀書計畫 #4 — 2026-05-01
主題重點：BST inorder 鞏固、Heap 經典、Graph topo sort 新 pattern、DP 經典、Tree Hard 挑戰

| # | 題目 | 難度 | 主題 | 為什麼選這題 |
|---|------|------|------|-------------|
| 1 | 230. Kth Smallest in BST | Medium | BST / Iterative Inorder | SR Review：earliest due（2026-04-14，17 天 overdue），Plan #1 ⚠️ 過。檢驗 iterative inorder 與 BST property 的肌肉記憶 |
| 2 | 973. K Closest Points to Origin | Medium | Heap | Google 校準：🔥🔥🔥 未做；Heap 主題 weak（題數<5、Plan #1 後沒再練）；經典 max-heap of size k pattern |
| 3 | 207. Course Schedule | Medium | Graph / Topological Sort | Weak：Graph BFS/DFS weak；🔥🔥🔥；**新 pattern 暖身**（topo sort 在 ANALYSIS 沒覆蓋過） |
| 4 | 300. Longest Increasing Subsequence | Medium | DP (1D) + BS | Weak：DP-1D weak（house-robber 8 次未自動化）；🔥🔥🔥；O(n²) DP + O(n log n) BS 兩種解法可做對比，**回顧 #1235 BS template 弱項** |
| 5 | 124. Binary Tree Maximum Path Sum | Hard | Tree / Recursion | Wildcard 挑戰：Tree weak（236 ⚠️ Plan #2）；🔥🔥🔥；經典 post-order 「向上回傳一條腿」pattern，跟 #236 LCA 思想對比 |

建議順序：先做 #1（BST 暖身）→ #2（Heap）→ #3（Graph 新 pattern）→ #4（DP+BS 鞏固）→ #5（Tree Hard 挑戰）
預估總時間：4-5 小時（Plan #4 起**開始單題計時**，目標：M < 25 min、H < 40 min）

**設計考量**
- Interleaving：BST → Heap → Graph → DP → Tree（5 個不同主題，無連續同主題 ✓）
- 難度分布：4 Medium + 1 Hard ✓
- 與 Plan #3 主題重疊：Plan #3 是 BS/UF/Stack/UF/DP+BS。Plan #4 重疊只有 DP（1/5=20%，在 40% 上限內 ✓）
- Weak 槽位：#3（Graph 新 pattern）、#4（DP-1D 鞏固）
- SR 槽位：#1（230 原題重做，最早 due）
- Google 校準槽位：#2（973 🔥🔥🔥 未做）
- Wildcard：#5（124 Hard + Tree weak + 🔥🔥🔥 三重命中）

**開始前的暖身問題**

#1（230 Kth Smallest）之前，回顧 `notes/iterative_inorder.md`：
1. Iterative inorder 的兩個 phase（push 階段 / pop+visit 階段）分別在做什麼？什麼時候從 phase 1 切到 phase 2？

#3（207 Course Schedule）之前（無既有筆記，需建立）：
2. Topological sort 的兩種寫法（Kahn's BFS / DFS post-order）你比較想學哪種？為什麼？

#4（300 LIS）之前，回顧 `notes/binary_search_templates.md`：
3. LIS 的 BS 版本中，BS 在「找位置 + 答案保證存在」還是「找特定值 + 可能不存在」？對應 Template A 還是 B？

#5（124 Max Path Sum）之前，回顧 #236 LCA 經驗：
4. Tree 遞迴向上回傳時，「子問題的答案」與「全局最大值」常需要分開維護。為什麼？

---

## 進度追蹤

| # | 題目 | 結果 | 花費時間 | 筆記 |
|---|------|------|----------|------|
| 1 | 230. Kth Smallest in BST (SR) | ⚠️ | — | 複雜度誤判（time O(n) 而非 O(h+k)）/ truthiness trap（val=0 時 `if resL` 會誤判）；commit 177ba78 |
| 2 | 973. K Closest Points to Origin | ✅ | — | max-heap of size k；pattern 熟。Gap：未主動講 max-heap 的「why」、quickselect 名稱、monotonic 性質 |
| 3 | 207. Course Schedule | ✅ | — | 首次接觸 Kahn's algorithm（BFS topo sort with in-degree），自己推導出演算法核心。Code 第一版有 2 處冗餘：(1) visited set 多餘（Kahn's 機制本身保證 node 不重複入 queue）(2) BFS-by-level（外層 while + 內層 for n in range）也多餘，topo sort 不需分 level。複雜度首次寫 O(E) 漏算 V，正解 O(V+E)。**DFS-based cycle detection（3-color）完全沒接觸過**，已記錄到 ANALYSIS P1 #7，下次 SR 重做要用 DFS 寫 |
| 4 | 300. Longest Increasing Subsequence | ⚠️ | 10 min | DP 與 BS 兩版都過 7 cases。DP 部分：dp[i] 定義初版模糊（「當前最長」未錨定 ending at i），需引導才寫出「以 nums[i] 結尾」+ 答案是 max(dp) 而非 dp[-1]。BS 部分：完全沒接觸過，從 mono stack 誤判（[0,1,0,3,2,3] counterexample）→ 帶 trace → 公式 → invariant 全程教學。**思路 3 段（tails 定義 / 為何遞增 / 為何覆寫安全）需多次精確化**，初版只用「紀錄最大長度 list」一句帶過。Code 細節對：bisect_left（strictly increasing 用左界）、append vs 覆寫分支正確。SR 重做要點：(1) 口述 tails[k] 精確定義（長度 + 最小結尾）(2) 為何 tails 嚴格遞增的結構性論證（砍結尾論證）(3) 覆寫安全的兩段（長度不變 + 未來潛力大）(4) 為何 bisect_left 不是 bisect_right |
| 5 | 124. Binary Tree Maximum Path Sum | ⚠️ | 10 min | Hard 但極快（< 40 min 目標）。Code 一次過 7 cases。**核心 insight 對**：分開維護「回傳值（一條腿）」與「全局 max（兩條腿在 node 折返）」+ 用 0 處理負腿。**Q-A/B 不對稱原因**口述對：「全局可折返用兩腿；回傳要讓 parent 線性接只能一腿」。**Q-C 負數**對：0 是「放棄這條腿」的選項。**Q-D self vs tuple**：選 self（具體理由：「明確標記跨節點累積，不混淆」）。**問題：思路書寫鬆**：初版寫「return [max, ...]」與 code 不符（code 用 self. 維護）；不對稱原因只在 chat 講清楚，未落到檔案，需多次提醒才補上。**Code 雖正確但繁瑣**：4 個候選 `max(self.tmpMax, resL+v+resR, v, v+resL, v+resR)` → 後改 `max(self.tmpMax, v+max(0, resL+resR, resL, resR))` 仍列舉 4 case。最乾淨應為 `gainL=max(resL,0); gainR=max(resR,0); v+gainL+gainR`（個別淨化腿）。**Clarifying questions 回答對**但沒主動提問，是被我列點問才答。**SR 重做要點**：(1) 用「個別淨化腿」寫法 (2) 思路寫進「不對稱」原因 (3) 主動問 clarifying（path 是否經過 root、單節點、負值） |
