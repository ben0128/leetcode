## 讀書計畫 #2 — 2026-04-18
主題重點：Tree/BST 對比學習、Stack 字串處理、2D DP 鞏固、Graph BFS 進階

| # | 題目 | 難度 | 主題 | 為什麼選這題 |
|---|------|------|------|-------------|
| 1 | 64. Minimum Path Sum | Medium | 2D DP | 接續 Plan #1 的 97（⚠️），用較單純的格子 DP 鞏固「狀態定義 + 邊界 + 滾動優化」流程 |
| 2 | 236. Lowest Common Ancestor of a Binary Tree | Medium | Tree | Tree 主題 weak（最近 2 題 ⚠️⚠️）；🔥🔥🔥 未做過；與 235 成 BST vs 一般 BT 對比 |
| 3 | 394. Decode String | Medium | Stack | Google 校準：🔥🔥🔥 未做過；雖然 Stack 主題 proficient，但 nested decode 是 Google 常見變體 |
| 4 | 235. Lowest Common Ancestor of BST | Medium | BST | SR Review：下次日期 2026-04-13（最久 due），複習 BST 性質 O(h) |
| 5 | 127. Word Ladder | Hard | Graph / BFS | 挑戰題：🔥🔥🔥 未做過；BFS shortest path + 字串變換的經典題，Google 常見 |

建議順序：先做 #1（2D DP 暖身）→ #2 → #3 → #4（SR 輕量 revisit）→ #5（挑戰）
預估總時間：4-5 小時

**設計考量**
- Interleaving：2D DP → Tree → Stack → BST → Graph，全部不連續同主題 ✓
- 難度分布：4 Medium + 1 Hard ✓
- 與 Plan #1 主題重疊：2D DP (64) + BST (235 review) = 2/5 = 40%（SR 複習是必要，在上限內）
- weak 槽位：#1 (2D DP 鞏固)、#2 (Tree 一般 BT LCA)
- Google 校準：#3 Decode String（🔥🔥🔥 未做）
- SR review：#4 LCA of BST
- wildcard/challenge：#5 Word Ladder

**開始前的暖身問題（2D DP 筆記複習）**
在做 #1 之前想一下這兩題：
1. 什麼情況你會用 2D DP 而不是 1D？（提示：看狀態有幾個獨立變量）
2. `dp[i][j]` 的定義必須包含什麼，才算「完整」？

---

## 進度追蹤

| # | 題目 | 結果 | 花費時間 | 筆記 |
|---|------|------|----------|------|
| 1 | 64. Minimum Path Sum | ⚠️ | — | 直接寫出 1D 滾動優化（優點），但 dp 定義/邊界/轉移需多次引導才精準；解釋「為什麼 1D work」講不出 index 對應（dp[j] 未覆寫=上一行、dp[j-1]=當前行左）。follow-up: 回傳 path 需改 O(m*n) space；稀疏 grid 轉 DAG O(K²) |
| 2 | 236. LCA of Binary Tree | ⚠️ | — | 核心 insight「左右都找到→root；只一邊→回傳那邊」講對，但第一版 code 有 bug（resL 非 None 就直接 return，沒遞迴右子樹）→ 失去 LCA 語意變「找第一個 p/q」。措辭精準度待加強（post-order vs inorder 混淆）。簡化後 6 行 solution。Follow-up 1 parent pointer 方向錯（說 cycle detector，實為 LL intersection LC 160） |
| 3 | 394. Decode String | ⚠️⚠️ | — | **重度卡關**。初始算法概念錯（數字字串混一個 stack + `]` 時只推答案 list 不能處理 nesting）。改 recursion 又卡 index 管理。最後寫 iterative 版仍有 2 個 bug：(1) `[` 時 push `[num, '']` 丟失外層 char；(2) `]` 時只 push `[1, char*popN]` 不用 popC。根本問題：**無法 visualize 嵌套處理**（試圖同時想所有層）。**標記為高優先重練 — 下次 /study 主動納入同系列（227/726）** |
| 4 | 235. LCA of BST (SR) | — | — | — |
| 5 | 127. Word Ladder | — | — | — |
