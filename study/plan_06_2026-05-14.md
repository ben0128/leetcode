## 讀書計畫 #6 — 2026-05-14
主題重點：Monotonic Queue SR Hard、BS 紀律重做、DP 1D + Hash 混合、Tree→Graph 轉換、Trie gap 首次

| # | 題目 | 難度 | 主題 | 為什麼選這題 |
|---|------|------|------|-------------|
| 1 | 153. Find Minimum in Rotated Sorted Array | Medium | Binary Search | weak（最近 2 題 ⚠️⚠️，BS template 仍未穩）；🔴 ANALYSIS 已預定作為 **#33 紀律重做題**（同 rotated 結構、純找 min、不涉 target 比較）；🔥🔥；**Template A 純練 + 紙筆 trace 邊界紀律訓練** |
| 2 | 139. Word Break | Medium | DP 1D + Hash Set | weak（DP-1D house-robber 歷史 8 次掙扎）；🔥🔥🔥 Google 高頻；**DP + hash set 混合 pattern 首次**（dp[i] = s[0:i] 可否被切分） |
| 3 | 208. Implement Trie (Prefix Tree) | Medium | Trie | **gap：題數 0 首次**；🔥🔥🔥；經典 design 題（dict-based vs node-based 結構選擇 + insert/search/startsWith 三 API） |
| 4 | 863. All Nodes Distance K in Binary Tree | Medium | Tree → Graph + BFS | Google 校準：🔥🔥🔥 未做；**tree-as-graph 轉換 pattern**（parent pointer 建邊 → BFS from target）；Tree weak + Graph BFS weak 雙覆蓋 |
| 5 | 239. Sliding Window Maximum (SR) | Hard | Monotonic Queue | **SR 29 天 overdue**（Active 表 earliest due，2026-04-15）；Plan #1 ⚠️（兩個實作 bug）；Monotonic Queue weak（題數<3）；雙重命中 weak + SR |

建議順序：先做 #1（BS 紀律暖身）→ #2（DP 新混合）→ #3（gap 首次）→ #4（multi-step Tree+Graph）→ #5（SR Hard 挑戰）
預估總時間：2.5-3 小時（4 M + 1 H；目標：M < 25 min、H < 25 min for SR）

**設計考量**
- Interleaving：BS → DP → Trie → Tree+Graph → Monotonic Queue（5 個不同主題，無連續同主題 ✓）
- 難度分布：4 Medium + 1 Hard ✓（≥3 M ✓、≥1 H ✓）
- Plan #5 主題重疊檢查：
  - P5 主題：Tree inorder / BS / DP-Knapsack / Hash+Prefix Sum / Sliding Window
  - P6 主題：BS / DP-1D / Trie / Tree+Graph / Monotonic Queue
  - 重疊：BS（不同題：33 vs 153，同 Template A 純練）、DP（不同子類：Knapsack vs 1D+Hash）、Tree（不同題：94 純 traversal vs 863 轉 graph）= 3/10 = 30% ≤ 40% ✓
- Weak 槽位：#1（BS）、#2（DP-1D）、#5（Monotonic Queue + SR 雙重命中）
- SR 槽位：#5（239 earliest due 29 天）
- Google 校準槽位：#4（863 🔥🔥🔥 未做）
- Gap/Wildcard：#3（Trie 0 題首次）

**開始前的暖身問題**

#1（153 Find Min Rotated）之前，回顧 `notes/binary_search_templates.md`：
1. Template A vs B 的判斷規則？這題是「找位置 + 答案保證存在」還是「找特定值 + 可能不存在」？為什麼？
2. 在 rotated 陣列上找最小，invariant 該怎麼定？`nums[mid]` 跟誰比較才能丟一半？（提示：**不是**跟 target 比，這題沒有 target）
3. 動 code 前**紙筆 trace** 兩個邊界 case：`[3,4,5,1,2]`（pivot 在中間）、`[1,2,3]`（未 rotate）——這是 #33 反覆強調的紀律。

#2（139 Word Break）之前（無既有筆記，需建立）：
4. 如果定義 `dp[i] = s[0:i] 可否被切分`，base case 是什麼？轉移時，給定 `i`，要往「過去哪些 `j`」查 `dp[j]`？條件是什麼？
5. wordDict 用 list 還是 set？為什麼？

#3（208 Implement Trie）之前（無既有筆記，需建立）：
6. TrieNode 的兩個核心欄位是什麼？（提示：一個記「往下走」、一個記「自己是不是終點」）
7. `search` 和 `startsWith` 的差別在哪一行 code？

#4（863 Distance K）之前：
8. 為什麼 binary tree 不能直接從 target 開始 BFS 找距離 K 的 nodes？少了哪個方向？
9. 怎麼補上這個方向？（提示：第一步要建立一個 mapping）

#5（239 Sliding Window Max SR）之前，回顧 Plan #1 #5 筆記：
10. 為什麼用 monotonic **decreasing** queue？front 永遠是當前 window 的 max？
11. 加入新元素時，從 back pop 的條件？前面所有比新元素**小或等**的都要 pop——為什麼可以安心丟掉？
12. 當 window 滑動，front 何時要 pop？這是 deque 不是 stack 的關鍵理由——你能講出來嗎？

---

## 進度追蹤

| # | 題目 | 結果 | 花費時間 | 筆記 |
|---|------|------|----------|------|
| 1 | 153. Find Minimum in Rotated Sorted Array | ✅ | ~10 min | BS Template B 一次過 6 cases。**#33 紀律修復成功**：兩個邊界 case `[3,4,5,1,2]` / `[1,2,3]` 主動紙筆 trace（寫進 code comment），未要求就執行。思路寫了兩分支 + `nums[l]` 反例 `[1,2,3]` 破解。**Gap**：思路沒講 Template B invariant（為何 `r = m` 不 infinite loop：`m=(l+r)//2 < r` when `l<r` → 區間必縮小）。Q2 一開始選對 `nums[r]` 但理由「確認那邊有排序」太淺，靠追問補出 `nums[l]` 反例論證。 |
| 2 | 139. Word Break | ⚠️ | ~15 min | DP+Hash 混合首次。Code 一次過 6 cases（含 tricky `"aaaaaaa"` greedy trap）。Push-style 寫法：跳過 `dp[i]=False` 位置。**Q4b/c 需引導**：「枚舉最後一段 + 前面切得開 + OR 語意」用具體 example "leetcode" → dp[8] scaffolding 才理解，自己想不到。**Q5 ✓**（set O(1) lookup）。**主動問「能用 trie 嗎」**——pattern matching 信號好，但留給 #3 做完再回來改 139。**思路偏薄**（一句話帶過，沒分 dp 定義 / base / 切點 OR / push-skip 4 段）。**複雜度 O(n³) 對**但漏 wordSet 空間。**紀律退步**：Q6 紙筆 trace 沒做（#1 連續性斷一格），code 一次過救了沒爆炸。 |
| 3 | 208. Implement Trie (Prefix Tree) | — | — | — |
| 4 | 863. All Nodes Distance K in Binary Tree | — | — | — |
| 5 | 239. Sliding Window Maximum (SR) | — | — | — |
