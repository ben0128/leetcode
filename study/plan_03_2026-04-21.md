## 讀書計畫 #3 — 2026-04-21
主題重點：Stack nested parsing 鞏固、Union Find 肌肉記憶、DP + Binary Search 進階

| # | 題目 | 難度 | 主題 | 為什麼選這題 |
|---|------|------|------|-------------|
| 1 | 162. Find Peak Element | Medium | Binary Search | Google 校準：🔥🔥🔥 未做；BS proficient 暖身，non-monotonic 陣列上的 BS 變體，Google 常考 |
| 2 | 547. Number of Provinces | Medium | Union Find | SR Review：下次日期 2026-04-14（tied earliest due），UF template 鞏固 |
| 3 | 227. Basic Calculator II | Medium | Stack | Weak：Stack 主題 weak（394 ⚠️⚠️ 重度卡關）；ANALYSIS Plan #2 結論指定下次 /study 納入；🔥🔥🔥 |
| 4 | 721. Accounts Merge | Medium | Union Find | Weak：UF 主題 weak（題數<3 未鞏固）；🔥🔥🔥 Google；ANALYSIS P1 建議；UF + 字串合併的典型進階變體 |
| 5 | 1235. Maximum Profit in Job Scheduling | Hard | DP + Binary Search | 挑戰題：🔥🔥🔥 Google；DP weak 進階；經典 weighted interval scheduling（sort + BS + DP 三層疊合） |

建議順序：先做 #1（BS 暖身）→ #2（UF SR 輕量）→ #3（Stack 重建 nested model）→ #4（UF 進階）→ #5（DP+BS 挑戰）
預估總時間：4-5 小時

**設計考量**
- Interleaving：BS → UF → Stack → UF → DP/BS；UF 被 Stack 分開，非連續 ✓
- 難度分布：4 Medium + 1 Hard ✓
- 與 Plan #2 主題重疊檢查：Stack 1/5（394→227 同系列刻意回練，鞏固 nested parsing mental model）= 20%，在 40% 上限內
- Weak 槽位：#3（Stack 鞏固）、#4（UF 進階）
- SR Review 槽位：#2（547 原題重做，UF template 暖機）
- Google 校準槽位：#1（162 🔥🔥🔥 未做）
- Wildcard/Hard：#5（1235，DP weak + 🔥🔥🔥 + Hard 三重命中）
- UF 雙題（547 SR + 721 weak）是刻意設計：先用已解的 547 暖機 UF template，再挑戰 721 的 string-based variant

**開始前的暖身問題**

#3（227 Basic Calculator II）之前，回顧 `notes/nested_parsing_stack.md`：
1. 394 的心智模型「抽屜+桌面」指的是什麼？遇到 `[` 時「抽屜」收什麼、「桌面」放什麼？
2. 227 沒有括號 nesting，為什麼還是需要 stack？（提示：運算符優先級怎麼模擬「延後處理」）

#2、#4（UF）之前，回顧 `notes/union_find_template.md`：
3. Union by rank 的 rank 語義到底是什麼？跟 size 的差別？
4. Path compression 兩種寫法（recursive 一行 vs iterative 兩段）你面試時會選哪個，為什麼？

---

## 進度追蹤

| # | 題目 | 結果 | 花費時間 | 筆記 |
|---|------|------|----------|------|
| 1 | 162. Find Peak Element | ⚠️ | — | BS on non-monotonic array：invariant（升→右、降→左）需引導；Template A vs B 一開始混淆；equality follow-up 需多次 trace 才理解 BS 在 == 時崩潰 |
| 2 | 547. Number of Provinces (SR) | ⚠️ | — | 從首次 ✅ 退步。3 個 UF template bug：find 用 `==` 漏 path compression（chain >1 會無限迴圈，剛好測資都淺樹沒爆）、union by rank 比 leaf 而非 root、掛 leaf 而非 root（會拆樹）。需 review 才抓出 → 維持 SR 2d。Template 肌肉記憶仍未穩固 |
| 3 | 227. Basic Calculator II | ⚠️ | — | 第一版用「看到 +/- 就 drain」太繞且 `len(stack) > 2` 邊界錯，`1-1+1` 算成 -1。重寫成 prev_op pattern：+/- 延後（push num / -num）、*// 立即（修改 stack[-1]）。雷點：用 `int(a/b)` 不是 `a//b`（題目要 truncate toward zero，遇負數會炸）。Code review：`n=len(s)` dead code、`enumerate` 不需要、helper 命名從 pushStack 改成 applyOperators。Follow-up O(1) space 已完成（res + lastNum 取代 stack）；曾誤用 closure 的 `num` 而非參數 `value`，測資沒爆但概念錯，已修 |
| 4 | 721. Accounts Merge | — | — | — |
| 5 | 1235. Maximum Profit in Job Scheduling | — | — | — |
