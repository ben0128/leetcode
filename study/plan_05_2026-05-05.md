## 讀書計畫 #5 — 2026-05-05
主題重點：Tree inorder SR、BS Template B 鞏固、DP Knapsack 新 pattern、Hash + Prefix Sum、Sliding Window Hard

| # | 題目 | 難度 | 主題 | 為什麼選這題 |
|---|------|------|------|-------------|
| 1 | 94. Binary Tree Inorder Traversal | Easy | Tree / Iterative Inorder | SR Review：21 天 overdue（pool 中最早 due，2026-04-14），Plan #1 ⚠️ 過。檢驗純 stack-based inorder 機制（與 230 BST kth 不同肌肉，不依賴 BST 性質） |
| 2 | 33. Search in Rotated Sorted Array | Medium | Binary Search | weak（Plan #4 #4 BS 部分仍卡 Template 區分）；🔴 ANALYSIS 明確指定下次 study 納入；🔥🔥🔥；**Template B + invariant 進階**（mid 落在哪一半的判斷） |
| 3 | 322. Coin Change | Medium | DP (Knapsack 系列) | weak（DP-Knapsack 題數<3，partition 5 次）；🔥🔥🔥；**unbounded knapsack 經典**（外層 amount / 內層 coins）；與 Plan #4 LIS 1D 不同 pattern |
| 4 | 560. Subarray Sum Equals K | Medium | Hash + Prefix Sum | Google 校準：🔥🔥🔥 未做；prefix sum + hashmap 經典；補強「count subarrays with property X」直覺 |
| 5 | 76. Minimum Window Substring | Hard | Sliding Window | Wildcard：🔥🔥🔥 經典 Google Hard；**sliding window 在 P3/P4 從未練過**（補主題缺口）；shrink-while-valid 進階 pattern |

建議順序：先做 #1（SR 暖身、Easy）→ #2（BS Template 鞏固）→ #3（DP 新 pattern）→ #4（Google 校準）→ #5（Hard 挑戰）
預估總時間：3-4 小時（Plan #4 已建立計時習慣；目標：M < 25 min、H < 40 min）

**設計考量**
- Interleaving：Tree → BS → DP → Hash → Sliding Window（5 個不同主題，無連續同主題 ✓）
- 難度分布：1 Easy + 3 Medium + 1 Hard ✓（≥3 M ✓、≥1 H ✓）
- Plan #4 主題重疊檢查：
  - P4 主題：BST inorder / Heap / Graph topo / DP+BS / Tree
  - P5 主題：Tree inorder / BS / DP-Knapsack / Hash / Sliding Window
  - 重疊：DP（不同子類，1D LIS vs Knapsack）、BS（不同類，DP+BS hybrid vs 純 BS Template B）、Tree（94 vs 124）= 3/10 ≤ 40% ✓
- Weak 槽位：#2（BS Template 鞏固）、#3（DP-Knapsack 新子類）
- SR 槽位：#1（94 原題重做，最早 due 21 天）
- Google 校準槽位：#4（560 🔥🔥🔥 未做）
- Wildcard：#5（76 Hard + Sliding Window 新主題 + 🔥🔥🔥 三重命中）

**開始前的暖身問題**

#1（94 Inorder）之前，回顧 `notes/iterative_inorder.md`：
1. Iterative inorder 的兩個 phase（push 階段 / pop+visit 階段）切換條件是什麼？為什麼 left 走完才 visit？

#2（33 Search Rotated）之前，回顧 `notes/binary_search_templates.md`：
2. Rotated array 中，每次拿 mid 後，**「左半 vs 右半哪一半是 sorted 的」** 怎麼判斷？確認 sorted 那半後，target 在不在這半要怎麼判？這是 Template A 還是 B？為什麼？

#3（322 Coin Change）之前（無既有筆記，需建立）：
3. Knapsack 0/1 vs Unbounded 的差別？dp 轉移迴圈順序的差別在哪？

#4（560 Subarray Sum K）之前：
4. Prefix sum + hashmap 怎麼把「subarray sum = k」轉成「兩個 prefix 差 = k」的問題？hashmap 存什麼？

#5（76 Min Window）之前：
5. Sliding window 中「shrink」與「expand」的觸發條件分別是什麼？什麼時候更新答案？

---

## 進度追蹤

| # | 題目 | 結果 | 花費時間 | 筆記 |
|---|------|------|----------|------|
| 1 | 94. Binary Tree Inorder Traversal (SR) | ⚠️ | ~25 min | code 一次過 5 cases，但思路初版過薄。經 Socratic + 多選題引導升級到強化 invariant：「pop 時 stack 頂端 node 的左子樹保證已 visit 完」。下次 SR 要開口就講出 invariant，不需引導 |
| 2 | 33. Search in Rotated Sorted Array | ⚠️⚠️ | ~90 min | 結構觀察 + 「哪半 sorted」決策 + Template A 選擇都對。**核心弱項：trace 紀律**：(1) 第一版兩處外側邊界 strict `<` 漏掉 target = nums[l]/nums[r] 的 case（學生未做被明確要求的紙筆 trace 即跑 test，自填 test 故意不涵蓋邊界故 "All tests passed!" 假象）(2) 第二次過修，把指標 `m+1`/`m-1` 改成 `m` → infinite loop（「值比較邊界」與「指標移動邊界」是兩件事）。學生誠實承認兩次都未 trace。教學矯正：**外側邊界 `<=`（值還沒被排除）+ 指標一律 `m±1`（mid 已 return 排除，必須跳過避免 infinite loop）**。思路寫得偏薄，未明確區分兩種邊界角色 → SR 重做時要強化。**已排程 LC 153（同 rotated 結構、找 pivot）作下次紀律重做題** |
| 3 | 322. Coin Change | ✅ | < 25 min | Code 一次過 5 cases（含 `[186,419,83,408],6249→20` greedy-fail edge）。**思路初版偏薄（同 #2 弱項）**，二版補完：base case 語意（「amount=0 by definition」非反推 example 3）+ sentinel `float('inf')` 選擇 + `dp[i-coin]+1` 的「上一步加一枚」semantic。**Trace 紀律進步**：先只 trace `[2],3` 偷懶，被點名後補 `[1,2,5],5` 三層演進正確。Loop order 選 outer coins / inner amount（對 322 兩者等價，**但 LC 518 combinations 會差** — 已記彩蛋）。`range(coin, amount+1)` 邏輯對：避免 negative index silent bug + 省 loop。**FU1 reconstruction**：第一直覺「dp[i] 存 list」可行但 O(A²/min_coin) space，引導到 parent pointer pattern `dp[i]=(count, last_coin)` → O(A) extra（72/1143 通用）。**FU2 BFS framing**：誤想 Dijkstra（uniform weight 不需 PQ），「BFS space 多」分析錯（加 visited 後同級）。BFS 早停場景需具象例子才懂（amount=10000、coins=[1,5,100,2500]、最佳=4 → 只展 4 levels；DP 仍要算 10001 cells）。下次練習鋪：LC 518 對照 loop order、Word Ladder/01 Matrix 類最短路徑用 BFS 框架 |
| 4 | 560. Subarray Sum Equals K | ⚠️ | < 25 min | Code 一次過 7 cases（含 `[0,0,0],k=0→6`、`[1,-1,0],k=0→3`、含負數 case）。**進步**：(1) **主動問 clarifying「array 是 sorted 嗎」**——這是 Google 面試該有的本能，#33/#322 都沒做到 (2) **主動 trace `[1,1,1],k=2`**（hashmap 演進 + count 累加）不需被點名，紀律比 #33/#322 進步。**Misconception**：sliding window 適用條件第一直覺講「sorted」，正解是「**所有元素 ≥ 0**」（用 sorted-with-negatives 反例 `[-3,-1,2,5]` 破解 → prefix sum 非單調，expand/shrink 邏輯崩）。**Q4 prefix 等式 `sum(nums[i..j]) = prefix[j+1] - prefix[i]` 對**；Q5 lookup `P_curr - k` 需要 nudge 才精準寫出。**Q6 因果倒了**：init `{0:1}` 寫對但解釋成「nums[0]..nums[j] 全選」（這是效果），正解是「**空 prefix（0 個元素和=0）的預設狀態存在 1 次**」（這是原因）→ 用對但沒真懂。**Code 可優化（未做到）**：用了 `prefix=[0]*(n+1)` array 存全部前綴，但只需單一 int 即可（過去 prefix 已由 hashmap 記住）→ canonical 解法是 `seen={0:1}; cur=0; for x in nums: cur+=x; res+=seen[cur-k]; seen[cur]+=1`，extra space O(1) 不算 hashmap。**思路仍偏薄**：一句話帶過，沒分 4 段（base/hashmap 內容/lookup 公式/為何不需 prefix array）。SR 重做要點：(1) sliding window 條件講「非負」不是「sorted」 (2) 開口直接用 single-int 版本，不要再開 prefix array (3) `{0:1}` 解釋「empty prefix 預設」不是「全選」 (4) 思路 4 段書面化 |
| 5 | 76. Minimum Window Substring | — | — | — |
