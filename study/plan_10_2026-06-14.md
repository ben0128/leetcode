# 讀書計畫 #10 — 2026-06-14

**類型：正常結構（2 weak + 2 review + 1 Google 校準）**
> SR backlog 仍 ≥ 12，但 Plan #9 已是 SR-burndown，規則「最多連續 1 份 burndown」→ 本份回正常結構，但用「due 5–11 的 2 review 槽」版本（吃掉 wildcard）持續清 backlog。

主題重點：Union Find / Stack-parsing 兩題最老 SR backlog（overdue 45–46 天）；Heap 弱項鞏固；DP-Knapsack 弱項（#322 的 loop-order 對照題）；Google 🔥🔥🔥 String 校準（edge-case discipline 硬題）。全程避開 Plan #9 主題（BST/2D-DP/Tree/BS/Graph）。

| # | 題目 | 難度 | 主題 | 為什麼選這題 |
|---|------|------|------|-------------|
| 1 | 547. Number of Provinces | M | Union Find | **SR review，overdue 46 天（最老）**。暖身。卡點史：UF template 3 bug（find 用 `==` 漏 path compression、union by rank 比 leaf 而非 root、掛 leaf 而非 root），簡單測資沒爆出來。重做要點：開口直接 canonical find（path-halving）+ union（先 find 兩 root）|
| 2 | 621. Task Scheduler | M | Heap / Greedy | **weak（Heap，Google 🔥🔥）**。新題。Heap 主題鞏固——但這題有 heap 與 greedy-math 兩種解，正好練「想到幾種解、各自複雜度」。重點：先講 clarifying（冷卻時間定義）+ 兩種思路對比 |
| 3 | 227. Basic Calculator II | M | Stack / Parsing | **SR review，overdue 45 天**。卡點史：自創「看到 +/- 就 drain」太繞、`1-1+1` 崩；核心 insight +/- 延後、*// 立即。重做要點：**從 O(1) space 版起步**（res + lastNum），不是 stack 版；truncate toward zero 用 `int(a/b)` 不是 `a//b` |
| 4 | 518. Coin Change II | M | DP / Knapsack | **weak（Knapsack）+ ANALYSIS 明確標「待做」**。這是 #322 Coin Change 的 loop-order 對照題（#322 記的彩蛋：外 coins/內 amount = **combinations**；反過來 = **permutations**）。重點：講出為什麼這題 loop order 不能交換 |
| 5 | 68. Text Justification | H | String | **Google 🔥🔥🔥 校準 + 挑戰題**。phone-screen 主力題型。這題沒有演算法難度，**全部重點是 corner cases（尾行左對齊、單字行、額外空格平均分配）+ clean code**——正中你的 precision / edge-case / test 紀律弱項，是 L4「production-ready、every corner case」bar 的試金石 |

建議順序：先做 **#1（547 暖身）→ #2（621）→ #3（227）→ #4（518）→ #5（68 挑戰）**
> interleaving 檢查：UF → Heap → Stack → Knapsack → String，無同主題相鄰。難度：4 M + 1 H（達標 ≥3 medium、≥1 hard）。
> 跨計畫 interleaving：Plan #9（BST/2D-DP/Tree/BS/Graph）vs Plan #10（UF/Heap/Stack/Knapsack/String）零主題重疊。

預估總時間：~2.5 小時（複習達標：Medium < 15 min、Hard < 25 min；#68 是實作硬題，時間放寬）

---

## 本計畫特定加強重點（常駐 forcing function 由 practice.md 擁有，這裡只記本份重點）

延續 Plan #9 三個剛暴露的教訓，這份繼續鎚：
1. **複雜度 Space 也要算「字串/物件佔多少」**（#127 暴露：Time 記得、Space 忘了 O(M·N)→寫成 O(M)）。每題 Space 開口先想「我存的每個元素佔多少」。
2. **事前 test gate 要口述「打哪個失敗點」**（目前只做到「有加 test」）。#68 尤其關鍵——它的 corner cases 就是評分點。
3. **clarifying 問改變解法形狀的保證**（#236/#162 教訓：別問不影響演算法的）。
4. **#235 仍是 chronic**（BST ordering + iterative O(1) 連三次需引導）：本份沒排，但下次 burndown 必選、再犯視同 Fail 重置。

---

## 進度追蹤

| # | 題目 | 結果 | 花費時間 | 筆記 |
|---|------|------|----------|------|
| 1 | 547. Number of Provinces | — | — | — |
| 2 | 621. Task Scheduler | — | — | — |
| 3 | 227. Basic Calculator II | — | — | — |
| 4 | 518. Coin Change II | — | — | — |
| 5 | 68. Text Justification | — | — | — |

> 每完成一題更新此表（結果 ✅/⚠️/❌、花費時間、筆記），並依 Update Protocol 維護 `review/schedule.md`（SR 複習題依 outcome 升降階；新題依收緊後排程納入規則）。
> 全 5 題完成後 append 摘要到 `ANALYSIS.md`「讀書計畫紀錄」。
