# 讀書計畫 #11 — 2026-07-01

**類型：SR-burndown（5 題全複習，不出新題）**
> 今日 due = 38 題（≥ 12）→ 觸發 burndown。Plan #10 為正常結構（非 burndown），「最多連續 1 份 burndown」限制不擋 → Plan #11 可為 burndown。
> **選題原則**：most-overdue 優先（overdue > 30 排最前）+ 榮譽兩個明確 chronic 必選（#235 explicit「下次 burndown 必選」、#227 Plan #10「減法 bug 必修」）。within-plan interleaving 已滿足（無同主題相鄰）；cross-plan topic 重疊在 burndown 模式下放寬（強制取 most-overdue，非按主題挑）。

主題重點：清最久 backlog（#721/#1235 overdue 59 天、#973 56 天）+ 兩個 chronic 硬骨頭（#235 BST ordering「連三次需引導」、#227 減法 bug + 假 pass）。橫跨 Heap / BST / Stack / UF / DP-BS。

| # | 題目 | 難度 | 主題 | 為什麼選這題 |
|---|------|------|------|-------------|
| 1 | 973. K Closest Points to Origin | M | Heap | **SR review, overdue 56 天, 首解 ✅**。暖身 + retention check。卡點史：max-heap of size k 一次寫對，但 (1) 沒主動講 max-heap vs min-heap 直覺 (2) 不知 quickselect 名稱 (3) sqrt 跳過的數學依據講太籠統。重做要點：開口直接補這 3 點；可用 `heappushpop` 合併 sift |
| 2 | 235. Lowest Common Ancestor of BST | M | BST | **SR review, chronic「下次 burndown 必選」**。BST ordering + iterative O(1) **連三次（4/20、6/11、6/13）都需引導**。重做要點：開口第一句就要「BST ordering 比大小下沉 + iterative O(1)」——**再犯視同 Fail 重置**；split 條件 inclusive `q≤cur≤p` + 先 swap；複雜度 O(h)/worst O(n) skewed/Space O(1) |
| 3 | 227. Basic Calculator II | M | Stack / Parsing | **SR review, Plan #10 剛 Fail（6/16）, 減法 bug 必修**。卡點史：減法符號 bug **兩度重演** `res-=lastNum`（`10-2-3`→-11）+ **假 pass**（對稱測資抵銷）。重做要點：開口直接三變數 + **符號綁項上 `lastNum=-num`、結算永遠 `+=`**；`int(a/b)` truncate；事前 gate 必含非對稱 `10-2-3`/`10-6-3` |
| 4 | 721. Accounts Merge | M | Union Find / Hash Map | **SR review, overdue 59 天（並列最老）**。卡點史：跨行 union 機制講不出（hand-waving 三次才答出 emailToIdx 橋樑）+ **斷鏈 bug** `roots[i]=find(j)` 覆蓋自己 parent。重做要點：跨行 union 直接講 `emailToIdx[email]` 當橋樑；union 兩邊都 `find`（`roots[find(i)]=find(j)`）；find canonical path-halving |
| 5 | 1235. Maximum Profit in Job Scheduling | H | DP + Binary Search | **SR review, overdue 59 天（並列最老）+ 挑戰題**。卡點史：DP 定義初版錯（i 是時間 + forward push）、漏「不選」分支、偏移 1 沒貫徹。重做要點：DP state「前 i+1 jobs max profit」；選/不選雙分支；偏移 1 全程貫徹；`bisect_right(endTimes, s)` 直接當 dp index（bisect=0 時 dp[0]=0 自動 base case）|

建議順序：先做 **#1（973 暖身, 首解✅）→ #2（235 chronic）→ #3（227 剛 Fail）→ #4（721 斷鏈）→ #5（1235 Hard 挑戰）**
> within-plan interleaving：Heap → BST → Stack → UF → DP-BS，無同主題相鄰。難度：4 M + 1 H（達標 ≥3 medium、≥1 hard）。

預估總時間：~1.5–2 小時（複習達標：Medium < 15 min、Hard < 25 min）

---

## 本計畫特定加強重點（常駐 forcing function 由 practice.md 擁有，這裡只記本份重點）

### 🔴 最高優先——mock #02 剛暴露的 clarify→test 斷層（跨題套用）
mock #02（#994 Rotting Oranges）最大發現：**學生 clarifying 親口問出的 corner case（fresh-but-no-rotten），卻沒把它寫成 test**，bug 就從這漏過 + 假 pass。這是 ANALYSIS 既有假 pass 模式（#33/#210/#227）+ trace 紀律弱項的延續。
- **本份每題強制**：clarify / 事前 gate 講到的**每一個** case → **立刻寫成一個 assert**，跑過才說 done。
- **「done」的定義**：已試圖打爆自己（挑最狠 input）+ 實際跑過。連兩場 mock Testing = 2 分，這是最該投資的單點。

### 本份 chronic 硬骨頭（兩題）
1. **#235 再犯即 Fail 重置**：BST ordering + iterative O(1) 連三次需引導，開口第一句就要對，否則重置 2d + 下次 burndown 再必選。
2. **#227 減法 bug + 假 pass**：符號綁項上 `lastNum=-num`、結算永遠 `+=`；事前 gate 必含非對稱 case。同 #235 chronic 性質「心智模型講得出、翻不成 code」——這題練「不靠骨架從零起手」。

### 延續 Plan #9/#10 教訓
- **複雜度 Space 主動分 aux vs output**（#68/#127 盲點）：每題開口先想「我存的每個元素佔多少」。
- **思路 4 段一寫就到位 + 與 code 對齊**（多份 recurring）。

---

## 進度追蹤

| # | 題目 | 結果 | 花費時間 | 筆記 |
|---|------|------|----------|------|
| 1 | 973. K Closest Points to Origin | ⚠️ | 10 min | Max-heap 核心零提示答對（"要 pop 最遠點所以要 max-heap"）+ 主動用 `heappushpop` 合併 sift + 主動加 origin 重合點 edge case。但 redo 3 點仍有 2 點需提示：sqrt 單調性追問兩次才講精確；follow-up O(n) 完全不會，追問後發現 **quicksort/partition 機制整個沒學過**（曾把 quicksort/quickselect 行為講反，之後自己修正對）。新增 `notes/quickselect_partition.md` + ANALYSIS 新 gap 主題。SR 維持 2d |
| 2 | 235. Lowest Common Ancestor of BST | ⚠️ | 43 min (wall) | chronic「開口第一句」**半收**：BST ordering 比大小下沉**零提示自己講出**（四次以來首次）；但 iterative O(1) 仍需引導（初版 DFS + SC O(h)），且**答完「用迴圈」卻寫出遞迴**（模型→code 翻譯 chronic 再現），點名 + 10^5 skewed RecursionError 才改。依 plan 預告規則視同 Fail → 重置 2d。invariant 迴避 3 輪（初版講成 #236 兩側遞迴，骨架矛盾自己發現）、trace 梯子後仍漏「含 curr 自身」由助教收；「split 為何最低」循環論證助教收。**docstring gate 破**（思路/複雜度留 TODO 收工）、follow-up 未做。亮點：swap + inclusive split 一次寫對；自加跨側 test p5（p=2,q=7）、q5 導航 bug 點名後自修；TC skewed O(n) 一問即答。新增 `notes/invariant_thinking.md` |
| 3 | 227. Basic Calculator II | ⚠️ | 20 min | 正確 + **減法不變量一次寫對**（`prev=-curr` 符號綁項、結算永遠 `+=`）、非對稱 gate `10-6-3` 主動入 assert、8 個 adversarial（含 `10-2-3`）全過——chronic 減法 bug 未再犯。但 20 min 超時（複習標準 <15）+ **有翻舊骨架**（舊解留在檔案裡）→「不靠骨架從零起手」目標未驗證。自發優化 ops dict 反引入雙重否定倒退（`ops['-'](res,-prev)` 模擬 `+=`），sentinel 收尾提示兩次未接。SR 維持 2d（07-10），下次**空白檔冷起手** |
| 4 | 721. Accounts Merge | — | — | — |
| 5 | 1235. Maximum Profit in Job Scheduling | — | — | — |

> 每完成一題更新此表（結果 ✅/⚠️/❌、花費時間、筆記），並依 Update Protocol 維護 `review/schedule.md`（SR 複習題依 outcome 升降階）。
> 全 5 題完成後 append 摘要到 `ANALYSIS.md`「讀書計畫紀錄」。
